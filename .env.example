# Survicate Configuration
# Copy this file to .env and fill in your actual values

# Required: Survicate API key (Surveys Settings → Access Keys)
TAP_SURVICATE_API_KEY=your_api_key_here

# Optional: The earliest response date to sync (ISO 8601)
TAP_SURVICATE_START_DATE=2024-01-01T00:00:00Z
